# CLI URL Parser

A command-line tool that reads URLs from a pipe seperated CSV file
and prints out the HTTP status code with the request duration.

## Usage

In terminal:
```bash
python download.py -i urls.csv
```
